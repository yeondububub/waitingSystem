spring:
  config:
    activate:
      on-profile: test
  data:
    redis:
      host: localhost
      port: 63790

scheduler:
  enabled: true
  max-allow-user-count: 1000

queue:
  token:
    secret-salt: "test-salt-key"

